Push the current branch.

Before pushing:

- Ensure working tree is clean.
- Confirm no failed tests.
- Show the branch name.

Execute:

git push origin <current-branch>

Display the remote URL and latest commit hash.